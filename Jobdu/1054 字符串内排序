#include <stdio.h>
#include <stdlib.h>
#include <algorithm>
#include <cstring>

using namespace std;

int main(void) {
    char input[200];
    while (scanf("%s", input) != EOF) {
        int len;
        len = strlen(input);
        sort(input, input + len);
        printf("%s\n",input);
    }

}
